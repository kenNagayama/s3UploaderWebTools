# ブランチ戦略を変更する

## 概要

- 開発用のメインブランチは `main` から `stg` に変更する
- backend を AWS にデプロイするのは `main` ブランチのみ
- 開発では、機能ごとに `f/xxxx` という名前のブランチを作成する
- 開発の成果は `f/xxxx` から `stg` にマージする
- `stg` でコードを確認した後、`stg` から `main` にマージする
  - 複数の開発成果を `stg` にマージしていき、まとめて `main` にマージする

